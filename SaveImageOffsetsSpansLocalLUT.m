%saves multiple versions of the same image, with multiple offsets and spans
%takes the original image in 0-2PI interval and applies offset and scale

function SaveImageOffsetsSpansLocalLUT(Image, Offsets, Spans, FileNamePrefix)
  
endfunction